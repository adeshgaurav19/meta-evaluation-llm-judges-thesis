# Push notes

Push `src/`, `scripts/`, `config/`, `report/` (tex + bib + images), `exports/` (CSVs + figures), `data/raw/`, `pipeline.py`, `pyproject.toml`, `README.md`.

Keep local only: raw judge outputs, model checkpoints, JSONL files, large datasets, `.env`.

See `.gitignore` for the exact exclusion rules.
